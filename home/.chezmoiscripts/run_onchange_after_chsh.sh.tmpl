{{ if not .gitpod -}}
#!/usr/bin/env bash
set -eufo pipefail

. "$(chezmoi source-path)/../lib/log.sh"

log_info "changing shell to zsh"
sudo chsh -s "$(which zsh)" "$(whoami)"
log_info_complete
{{ end -}}
